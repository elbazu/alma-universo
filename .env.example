# ─── PocketBase ───────────────────────────────────────────────────
# Public URL of the PocketBase instance. Must be reachable from the browser.
# Staging: https://momatwork-pocketbase-alma-staging.weupli.easypanel.host
# Production: https://momatwork-pocketbase-alma.weupli.easypanel.host
NEXT_PUBLIC_POCKETBASE_URL=https://momatwork-pocketbase-alma-staging.weupli.easypanel.host

# Notes on auth:
# - Google OAuth and Email OTP (magic links) are configured inside the
#   PocketBase admin UI, not via env vars. See Settings → Auth providers.
# - Keep Google client ID/secret + SMTP credentials in PocketBase, not here.
